# Agent Guidelines

- Do not expose raw/default browser UI for user-facing form controls. Use existing shared app components, or create a reusable styled component that follows the app design system before using native inputs directly.
